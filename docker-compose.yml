
volumes:
  hf_cache:
    driver: local

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - api/.env  # Load environment variables from .env
    volumes:
      - ./api:/app/api  # Mount API code for development (optional)
    restart: unless-stopped
    
   
    # depends_on:
    #   - db
    # db:
    #   image: postgres:15
    #   environment:
    #     POSTGRES_DB: defaultdb
    #     POSTGRES_USER: doadmin
    #     POSTGRES_PASSWORD: your_password
    #   ports:
    #     - "5432:5432"

