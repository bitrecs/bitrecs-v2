services:
  validator:
    build:
      context: ..
      dockerfile: validator/Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../:/app
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1